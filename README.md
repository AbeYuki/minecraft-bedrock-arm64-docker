# minecraft-bedrock-arm64-docker
Run the minecraft bedrock server in an ARM64 environment.

## Quick start

```
docker compose up -d
```

## build && up

```
cd build/
```
```
docker compose up -d
```